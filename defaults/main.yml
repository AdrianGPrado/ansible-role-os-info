# The directory where binaries are stored on Ansible managed systems.
bin_dir: /usr/bin

# The directory where scripts used for bootstrapping CoreOS reside.
bootstrap_script_dir: /opt/bin

# Boostrap Python version for CoreOS
pypy_version: 2.4.0
